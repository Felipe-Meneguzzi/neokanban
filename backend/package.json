{
  "name": "kanban-backend",
  "version": "1.0.0",
  "description": "Kanban Project Manager API",
  "main": "src/index.ts",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "pg": "^8.12.0",
    "hono": "^4.6.0",
    "@hono/node-server": "^1.13.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/pg": "^8.11.6",
    "@types/cors": "^2.8.17",
    "typescript": "^5.6.0"
  }
}

